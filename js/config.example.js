// 1. Đăng ký tài khoản miễn phí tại https://home.openweathermap.org/users/sign_up
// 2. Vào https://home.openweathermap.org/api_keys để lấy API key (chờ ~10-30 phút để key kích hoạt)
// 3. Copy file này thành "config.js" (cùng thư mục) và dán key vào bên dưới
// 4. KHÔNG commit config.js lên Git (đã có trong .gitignore)

const CONFIG = {
  API_KEY: "YOUR_OPENWEATHERMAP_API_KEY_HERE",
};
